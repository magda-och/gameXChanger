
package com.gt.gamexchanger.mapper;

import com.gt.gamexchanger.dto.FriendDto;
import com.gt.gamexchanger.model.Friend;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;
import static org.junit.jupiter.api.Assertions.*;

class FriendDtoMapperTest {
/*
    FriendDtoMapper friendDtoMapper;

    @Test
    void toDto() {
        Friend friend = new Friend(1L, 2L);
        friendDtoMapper = new FriendDtoMapper();
        FriendDto friendDto = friendDtoMapper.toDto(friend);

        assertThat(friendDto.getFirstUserId()).isEqualTo(1);
        assertThat(friendDto.getSecondUserId()).isEqualTo(2);

    }
    @Test
    void toDtoifFriendIsNull() {
        friendDtoMapper = new FriendDtoMapper();
        FriendDto friendDto = friendDtoMapper.toDto(null);

        assertNull(friendDto);

    }

    @Test
    void toDomainObject() {
        friendDtoMapper = new FriendDtoMapper();
        FriendDto friendDto = new FriendDto();
        friendDto.setFirstUserId(1L);
        friendDto.setSecondUserId(2L);

        Friend friend = friendDtoMapper.toDomainObject(friendDto);

        assertThat(friend.getFirstUserId()).isEqualTo(1);
        assertThat(friend.getSecondUserId()).isEqualTo(2);
    }*/

}
